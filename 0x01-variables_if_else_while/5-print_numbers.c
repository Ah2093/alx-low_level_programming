#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	for (int letter =0; letter <= 9; letter++)
		printf(letter);
	printf('\n');
	return (0);
}
